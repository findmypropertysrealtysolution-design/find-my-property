/** Stable import path — card in `modules/properties`; types in `@/lib/property-view-model`. */
export { default } from "@/modules/properties/PropertyCard";
export type { FurnishingStatus, Property } from "@/lib/property-view-model";
