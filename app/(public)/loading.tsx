import { RouteLoadingShell } from "@/components/skeletons/route-loading";

/** Shown while the public route segment streams (instant navigation feedback). */
export default function Loading() {
  return <RouteLoadingShell />;
}
